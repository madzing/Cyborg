package EvalAi;

import org.tensorflow.*;
import org.tensorflow.op.Ops;

public class test {

	
public test()
{
	try(Graph graph = new Graph()) {
	    Ops tf = Ops.create(graph);
	    
	    // Keras code here.
	}
}
}
